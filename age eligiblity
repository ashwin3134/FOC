#include <stdio.h>
int main()
{
    int age;
    printf("Enter age : ");
    scanf("%d", &age);
    
    if (age >= 18)
        printf("You are eligible to Vote!");
    else
        printf("You not elgible to Vote!");

    return 0;
}
