#include<stdio.h>
int main()
{
	int i,c,n;
	printf("Enter the number of rows in pyramid of stars to print\n");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		for(c=1;c<=n-i;c++)
		printf(" ");
		for(c=1;c<=2*i-1;c++)
		printf("*");
		printf("\n");
	}
	return 0;
}
